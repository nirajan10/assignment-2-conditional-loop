// Write a C program that prints all numbers from 1 to 100 using a for loop.

#include <stdio.h>

int main() {
    for (int i = 1; i <= 100; i++) {
        printf("%d ", i);
    }
    printf("\n");
    return 0;
}
